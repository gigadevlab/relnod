def pre_process(keys: [], token: str) -> ([], str):
    # TODO: Implement to make pre-process.
    print('PRE_PROC', token, flush=True)
    return keys


def post_process(rows: [], token: str) -> ([], str):
    # TODO: Implement to make post-process.
    print('POST_PROC', token, flush=True)
    return rows
